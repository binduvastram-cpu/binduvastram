// Placeholder admin gate — accepts any non-empty email/password since there's
// no real backend auth yet. Replace with real Supabase-backed admin auth in
// the backend phase.
export const ADMIN_SESSION_KEY = "bv-admin-session"
